#ifndef INTER_SCHEME_HPP
#define INTER_SCHEME_HPP

#include <array>
#include <cmath>

// 定义 real 类型
using real = double;

// 函数声明
real whf_TcnsN_A(std::array<real, 5> q);
real whf_ai_TcnsN_A_1(std::array<real, 5> q);
real whf_TcnsN_COMPARE(std::array<real, 5> q);


#endif // INTER_SCHEME_HPP
